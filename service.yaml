apiVersion: v1
kind: Service
metadata:
  name: my-service123
spec:
  selector:
    app.kubernetes.io/name: my-deployment123
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376